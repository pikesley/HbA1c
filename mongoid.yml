production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>

development:
  sessions:
    default:
      database: metrics_api_development
      hosts:
        - localhost

test:
  sessions:
    default:
      database: metrics_api_test
      hosts:
        - localhost
