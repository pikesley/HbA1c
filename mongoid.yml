production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>

development:
  sessions:
    default:
      database: metrics_api_development
      hosts:
        - localhost

test:
  sessions:
    default:
      database: metrics_api_test
      hosts:
        - localhost
